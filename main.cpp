/*
  Проект: Управление светодиодом с помощью кнопки
  Автор: Сергей Алеханов
  Описание: При нажатии на кнопку светодиод загорается,
            при отпускании — гаснет.
*/

// Пин, к которому подключен светодиод
const int ledPin = 13;

// Пин, к которому подключена кнопка
const int buttonPin = 2;

// Переменная для хранения состояния кнопки
int buttonState = 0;

void setup() {
  // Настраиваем пин светодиода на выход
  pinMode(ledPin, OUTPUT);
  
  // Настраиваем пин кнопки на вход
  pinMode(buttonPin, INPUT);
}

void loop() {
  // Читаем текущее состояние кнопки
  // HIGH — кнопка нажата, LOW — не нажата
  buttonState = digitalRead(buttonPin);
  
  // Если кнопка нажата, включаем светодиод
  if (buttonState == HIGH) {
    digitalWrite(ledPin, HIGH);   // Подаём напряжение — светодиод горит
  } 
  // Иначе выключаем
  else {
    digitalWrite(ledPin, LOW);    // Убираем напряжение — светодиод не горит
  }
}
